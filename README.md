# PR #463 live evidence — 16 September 2026

Chrome recordings for draft PR open-legal-products/mike#463
(`olp-pr/org-access-followup-main`, tip 452b954e) against `main` f024fe01.

`base/` is today's `main`; `fix/` is the branch. Same stack, same database,
same seeded users (`<name>@pr463.test`). Each flow keeps its key still as
`*-key.png` next to the GIF.

| Id | Flow |
|---|---|
| B01 | Directory-search picker and a denied org member |
| B23 | Your own row in the Access modal |
| B26 | "Delete organization?" confirm vs. its settings modal |
| B05 | Project wizard retry after a refused grant |
| B32 | Project-chat rename/delete gates in the in-place chat workspace |
